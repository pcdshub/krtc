package:
  name: krtc
  version: "0.0.3"

source:
  git_url: https://github.com/slaclab/krtc.git
  git_tag: "0.0.3"

build:
  script:
    - pip install --no-deps --disable-pip-version-check .

requirements:
  build:
    - python
    - setuptools
    - pykerberos
  run:
    - python
    - setuptools
    - pykerberos

about:
  home: https://github.com/slaclab/krtc.git
  license: MIT License
